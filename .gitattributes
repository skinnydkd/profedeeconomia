# Git already stores every text file with LF. Without this, a Windows checkout
# turns them into CRLF, and anything that parses its own source or its content
# expecting \n breaks there while passing on CI — which is exactly what made
# two test suites fail only on Windows.
* text=auto eol=lf
