# build
dist/
dist-verify/
.astro/

# generated slide skeletons — regenerated by `npm run build:slides`
# from the MDX of each unit. The PDFs and HTMLs under public/slides/
# ARE committed so the site can serve them without a rebuild.
tmp/

# deps
node_modules/

# env / secrets
.env
.env.production
.env.development
.env.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
!.vscode/tasks.json
.idea/

# vercel
.vercel/

# claude code local settings (machine-specific, not for the repo)
.claude/

# superpowers brainstorming visual companion (local mockups, not for the repo)
.superpowers/

# partykit local dev cache
.partykit/

# Cajút manifest (auto-generat per `npm run build:cajut-manifest`)
public/games-multi/cajut/manifest.json
party/cajut/questions.generated.json

# Jocs Econòmics banc (auto-generat per `npm run build:jocs-bank`)
public/jocs-economics/manifest.json
src/server-only/jocs-bank.json

# Webpde raw HTML cache (només per a inspecció manual del bootstrap)
scripts/__webpde-raw__/

# Bootstrap del webpde (Pau revisa i mou a preguntas/ amb format vàlid)
src/content/jocs-economics/_staging-webpde/

# Slides diagram captures (auto-generat per npm run capture:diagrams)
public/slides-assets/
tmp/slides-assets-manifest.json
tmp/slides-image-cache/
tmp/profedeeconomia.compiled.css
